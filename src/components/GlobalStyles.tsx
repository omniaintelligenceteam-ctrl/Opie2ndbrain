'use client'

// All global CSS has been consolidated into src/styles/premium.css
// which is imported in layout.tsx. This component is kept as a no-op
// to avoid breaking any stale imports.
export default function GlobalStyles(): React.ReactElement | null {
  return null;
}
